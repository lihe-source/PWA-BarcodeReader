# BarcodePro V3_0 驗證紀錄

日期：2026-09-05。檢查對象：本下載包；未部署或修改使用者的 GitHub Pages。

## 已完成

`node verify.cjs`：**20 組檢查全部通過**。

| 類別 | 已驗證行為 |
| --- | --- |
| 檔案完整性 | 所有 JS 可解析、HTML 本機資源存在、沒有重複 ID、程式引用的固定元素存在、Manifest 圖示存在 |
| 初始化與操作連接 | 以 DOM 替身模擬初始化、相機不可用時顯示錯誤、頁面切換、再次產碼帶回原格式與空白、草稿暫存與安全更新條件 |
| 資料移轉 | V2 前後空白／換行保留，V2 原資料仍存在 |
| 容量行為 | 移轉後共 551 筆資料全部保留，沒有 500 筆截斷 |
| 備份還原 | 重複匯入 550 筆不重複新增；含無效資料時整批拒絕，既有資料不變 |
| 儲存失敗 | 注入容量不足錯誤，交易回復、資料未新增、忙碌狀態解除 |
| 刪除與復原 | 原始 ID、讀值及空白保持一致 |
| 設定與檢核 | 多筆設定序列寫入、連續掃描強制保存、格式／長度／前綴檢核 |
| 外部網址及 CSV | 限完整 HTTP／HTTPS、排除 javascript:、不從任意內容抽取網址；公式與長數字有文字保護 |
| 產碼參數 | 八種正式產碼參數通過隨包 bwip-js 的渲染路徑；QR／Data Matrix 不傳 undefined 高度，PDF417 不傳 QR 專用 M 等級 |
| 條碼讀值 | QR、Data Matrix、PDF417、Code 128、Code 39、EAN-13、EAN-8、UPC-A：隨包產碼器 → 像素資料 → ZXing 回讀內容一致 |
| 檢查碼及版本 | EAN 補碼／錯誤拒絕，V3_10 大於 V3_2，底線／點版本可比較 |
| 快取及離線 | 必要資源完整安装；只刪本程式舊快取；其他專案快取保留；離線導覽讀快取；缺檔回傳有效 503；版本檢查離線時不回假最新版本 |

測試使用 Node.js、程式庫本身、DOM 替身、具交易回復行為的 IndexedDB 替身與 CacheStorage 替身。條碼回讀使用實際隨包編碼／解碼演算法，但不是攝影機拍攝。這些結果不代表已完成真實瀏覽器或行動裝置認證。

## 部署後的裝置驗收

以下項目**尚未於真實裝置驗證**，建議在正式工作資料使用前完成：

1. iPhone Safari／主畫面 PWA、iPad Safari／PWA、Windows Chrome：首次允許相機、拒絕後重試、切鏡頭、離開與返回背景。
2. 設定頁顯示離線資源完整後，開飛航模式，完全關閉再開啟，確認掃描、圖片及產碼仍可使用。
3. 使用真實不同尺寸條碼、反光標籤、相鄰條碼及模糊圖片，確認掃描框與選取內容符合操作預期。
4. 連續掃描相同碼：冷卻期間不重複計數，超過冷卻時間才新增；設定不符時不保存。
5. 測試音效、震動、補光燈與變焦，確認裝置能力及系統權限；不支援功能應停用或給出說明。
6. 手機直向、iPad 橫向／分割畫面、電腦及 200% 字體：確認表單、結果與底部導覽無遮擋。
7. 同一來源的多個 PWA 共存，安裝／升級 BarcodePro 後確認其他 PWA 仍可離線使用。
8. 真實 IndexedDB 中還原備份、重新開啟、再次匯出逐筆比對；不要用唯一一份正式資料測試清除。
9. 在測試副本建立下一版本，輸入草稿／開啟編輯時更新應延後；關閉輸入後套用並保留草稿與結果。
10. PNG 分享到檔案 App、標籤以 100% 比例列印，並用現場掃描器驗證實際尺寸、留白、讀值與格式。

實機通過前不宣稱辨識速度、辨識率、電池節省比例或跨裝置支援率。
